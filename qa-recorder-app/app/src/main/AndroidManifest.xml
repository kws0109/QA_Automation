<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <!-- 기본 권한 -->
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE_MEDIA_PROJECTION" />
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />

    <!-- 저장소 권한 -->
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE"
        android:maxSdkVersion="28"
        tools:ignore="ScopedStorage" />
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE"
        android:maxSdkVersion="32" />

    <!-- Android 13+ 미디어 권한 -->
    <uses-permission android:name="android.permission.READ_MEDIA_VIDEO" />
    <uses-permission android:name="android.permission.READ_MEDIA_IMAGES" />

    <application
        android:allowBackup="true"
        android:icon="@drawable/ic_launcher_foreground"
        android:label="@string/app_name"
        android:requestLegacyExternalStorage="true"
        android:supportsRtl="true"
        android:theme="@style/Theme.QARecorder"
        tools:targetApi="31">

        <!-- 메인 액티비티 -->
        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:launchMode="singleTop">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!-- 녹화 서비스 (Foreground Service) -->
        <service
            android:name=".RecorderService"
            android:exported="true"
            android:foregroundServiceType="mediaProjection">
            <intent-filter>
                <action android:name="com.qaautomation.recorder.START_RECORDING" />
                <action android:name="com.qaautomation.recorder.STOP_RECORDING" />
                <action android:name="com.qaautomation.recorder.GET_STATUS" />
                <action android:name="com.qaautomation.recorder.TAKE_SCREENSHOT" />
                <action android:name="com.qaautomation.recorder.MATCH_TEMPLATE" />
            </intent-filter>
        </service>

        <!-- ADB Broadcast 수신기 -->
        <receiver
            android:name=".CommandReceiver"
            android:exported="true">
            <intent-filter>
                <!-- 녹화 명령 -->
                <action android:name="com.qaautomation.recorder.START_RECORDING" />
                <action android:name="com.qaautomation.recorder.STOP_RECORDING" />
                <action android:name="com.qaautomation.recorder.GET_STATUS" />

                <!-- 스크린샷 명령 -->
                <action android:name="com.qaautomation.recorder.TAKE_SCREENSHOT" />

                <!-- 템플릿 매칭 명령 -->
                <action android:name="com.qaautomation.recorder.MATCH_TEMPLATE" />

                <!-- 서비스 제어 -->
                <action android:name="com.qaautomation.recorder.INIT_SERVICE" />
                <action android:name="com.qaautomation.recorder.STOP_SERVICE" />
            </intent-filter>
        </receiver>

        <!-- FileProvider for sharing files -->
        <provider
            android:name="androidx.core.content.FileProvider"
            android:authorities="${applicationId}.fileprovider"
            android:exported="false"
            android:grantUriPermissions="true">
            <meta-data
                android:name="android.support.FILE_PROVIDER_PATHS"
                android:resource="@xml/file_paths" />
        </provider>

    </application>

</manifest>
