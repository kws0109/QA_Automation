# ==============================================
# Frontend 환경 설정
# ==============================================

# 서버 호스트 (외부 접근 시 서버 IP/도메인 설정)
# 기본값: 현재 접속 중인 호스트 (window.location.hostname)
# 예: 192.168.1.100, qa-server.example.com
VITE_SERVER_HOST=

# Backend 포트 (기본값: 3001)
VITE_BACKEND_PORT=3001

# API URL (기본값: http://{SERVER_HOST}:{BACKEND_PORT})
# 전체 URL을 직접 지정하려면 아래 사용
VITE_API_URL=

# WebSocket URL (Socket.IO 연결용)
# 기본값: http://{SERVER_HOST}:{BACKEND_PORT}
VITE_WS_URL=

# WebSocket Stream URL (스크린 스트리밍용, ws:// 프로토콜)
# 기본값: ws://{SERVER_HOST}:{BACKEND_PORT}
VITE_WS_STREAM_URL=
